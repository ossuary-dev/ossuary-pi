#!/usr/bin/env python3
"""Ossuary API Service Executable."""

import sys
import os
import asyncio
from pathlib import Path

# Add source directory to Python path
sys.path.insert(0, "/opt/ossuary/src")

from api.service import main

if __name__ == "__main__":
    # API service can run as unprivileged user
    asyncio.run(main())