#!/usr/bin/env python3
"""Ossuary Portal Service Executable."""

import sys
import os
import asyncio
from pathlib import Path

# Add source directory to Python path
sys.path.insert(0, "/opt/ossuary/src")

from portal.server import main

if __name__ == "__main__":
    if os.geteuid() != 0:
        print("This service must be run as root")
        sys.exit(1)

    asyncio.run(main())